<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            margin: 0;
            padding: 20px;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h1, h2, h3 {
            text-align: center;
            margin-bottom: 10px;
        }

        .post-info {
            text-align: right;
            margin-bottom: 10px;
        }

        .post-info button, .comment button {
            padding: 8px 16px;
            font-size: 14px;
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        .post-info button:hover, .comment button:hover {
            background-color: #0056b3;
        }

        .comment {
            margin-top: 20px;
            padding: 10px;
            background-color: #f9f9f9;
            border: 1px solid #ddd;
            border-radius: 8px;
        }

        .comment .comment-info {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 10px;
        }

        .comment .comment-info button {
            background-color: #dc3545;
            transition: background-color 0.3s;
        }

        .comment .comment-info button:hover {
            background-color: #c82333;
        }

        footer {
            text-align: center;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>{{title}}</h1>
        <h2>제목입니다</h2>
        <div class="post-info">
            작성자 : <b></b>
        </div>
        <div class="post-info">
            조회수 : 999 | 작성일시 : 2022
            <button onclick="modifyPost()">수정</button>
            <button onclick="deletePost()">삭제</button>
        </div>

        <div>
            <pre>본문 내용</pre>
        </div>

        <section>
            <div>
                <h3>3개의 댓글이 있습니다.</h3>
            </div>

            <form method="post" action="/write-comment">
                <div>
                    <div>
                        <input type="text" name="name" placeholder="이름" />
                        <input type="password" name="password" placeholder="비밀번호" />
                    </div>
                    <div>
                        <textarea cols="40" rows="3" name="comment" placeholder="댓글을 입력해주세요."></textarea>
                        <br /><br /><button>댓글 쓰기</button>
                    </div>
                </div>
            </form>
        </section>

        <section class="comment">
            <div class="comment-info">
                <div>
                    작성자 : <b>댓글 작성자</b>
                </div>
                <div>
                    작성일시 : 2024
                    <button onclick="deleteComment('1')">삭제</button>
                </div>
            </div>
            <div>
                <pre>{{comment}}</pre>
            </div>
        </section>

        <footer>
            <div>
                <a href="/">목록으로</a>
            </div>
        </footer>
    </div>
</body>
</html>